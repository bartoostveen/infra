# derived from https://git.toostveen.nl/tom/puntbestanden/src/branch/hoofdlijn/.forgejo/workflows/update-kernels.yml

"on":
  schedule:
    - cron: "0 4/8 * * *"
      timezone: Europe/Amsterdam
  workflow_dispatch: {}

jobs:
  update-kernels:
    runs-on: [gpg, native-x86_64-linux]
    env:
      GIT_SSH_COMMAND: ${{ needs.setup.outputs.git-ssh-command }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-home
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
          ssh-known-hosts: ${{ vars.SSH_KNOWN_HOSTS }}
          gpg-privkey: ${{ secrets.GPG_PRIVKEY }}
          gpg-key-id: ${{ vars.GPG_KEY_ID }}

      - name: perform update
        id: update
        run: |
          git switch "$FORGEJO_REF_NAME"
          kver() {
            nix eval --inputs-from . --raw nixos-small#linuxPackages_latest.kernel.version --accept-flake-config
          }
          old="$(kver)"
          nix flake update --commit-lock-file nixos-small --accept-flake-config
          new="$(kver)"
          if [ "$old" != "$new" ]; then
            git commit --amend -m "chore: linux $old -> $new"
            echo continue=1 >> "$FORGEJO_OUTPUT"
          else
            echo continue=0 >> "$FORGEJO_OUTPUT"
          fi

      - name: send PR
        if: ${{ steps.update.outputs.continue == '1' }}
        run: |
          base="$(git branch --show-current)"
          if [[ "$(git rev-list origin/"$base"..HEAD --count)" -ne 0 ]]; then
            head="ci-update-kernel"

            existing_prs="$(
              curl --silent --fail-with-body \
                -H "Authorization: token $FORGEJO_TOKEN" \
                "$FORGEJO_SERVER_URL/api/v1/repos/$FORGEJO_REPOSITORY/pulls?state=open"
            )"

            if jq -e --arg head "$head" 'any(.[]; .head.ref == $head)' <<< "$existing_prs" > /dev/null; then
              echo "PR for branch '$head' already exists, exiting."
              exit 0
            fi

            commit_subject="$(git log -1 --pretty=%s)"

            git switch -c "$head"
            git push --force-with-lease origin "$head"
            
            jq -n \
              --arg base "$base" \
              --arg head "$head" \
              --arg title "chore: bump kernel" \
              --arg body "$commit_subject" \
              '{
                base: $base,
                head: $head,
                title: $title,
                body: $body
              }' |
            curl --fail-with-body "$FORGEJO_SERVER_URL/api/v1/repos/$FORGEJO_REPOSITORY/pulls" \
              -H "Authorization: token $FORGEJO_TOKEN" \
              -H "Content-Type: application/json" \
              --data @-
          fi